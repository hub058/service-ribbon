server:
  # ribbon服务的端口
  port: 8764
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      # 服务的注册中心地址
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
spring:
  application:
    # 服务名称
    name: service-ribbon

